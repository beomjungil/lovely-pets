import styled from 'styled-components';
import { Container } from '../../styles/baseComponents';

export const ListWrapper = styled(Container)`
  max-width: calc(1200px + 1.75rem);
  padding: 5rem 0;
  display: flex;
  flex-wrap: wrap;
`;
